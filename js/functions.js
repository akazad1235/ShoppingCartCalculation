   //plus 1 button funciton added
    function plusButton(input, currentPriceNumber){
        // var getValue = parseInt(input.value)
        //     getValue=getValue+1
        //     input.value=getValue
        //     totalPrice = getValue+currentPriceNumber
        //     return totalPrice
        
    }
    function minusButton(input){
        var getValues = parseInt(input.value)
            getValues-=1
            input.value=getValues   
    }
    
   